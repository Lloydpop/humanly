"use client";

import { motion } from "framer-motion";

const words = ["Get Started", "Sign Up", "Learn More", "Join Us", "Explore"];

export default function HeroScatterFramer() {
  return (
    <section className="relative h-[300px] my-10 w-[50%] mx-auto flex items-center justify-center bg-gradient-to-b from-purple-700 to-indigo-900 overflow-hidden">
    
    </section>
  );
}
